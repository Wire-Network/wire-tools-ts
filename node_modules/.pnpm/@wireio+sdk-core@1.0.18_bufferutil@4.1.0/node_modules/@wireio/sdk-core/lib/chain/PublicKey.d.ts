import { ABIDecoder } from "../serializer/Decoder";
import { ABIEncoder } from "../serializer/Encoder";
import { ABISerializableObject } from "../serializer/Serializable";
import { Bytes } from "./Bytes";
import { KeyType } from "./KeyType";
export type PublicKeyType = PublicKey | string | {
    type: string;
    compressed: Uint8Array;
};
export declare class PublicKey implements ABISerializableObject {
    static abiName: string;
    /** Type, e.g. `K1`, `R1`, `EM`, or `ED` */
    type: KeyType;
    /** Compressed public key point. */
    data: Bytes;
    /** Create PublicKey object from representing types. */
    static from(value: PublicKeyType): PublicKey;
    /** @internal */
    static fromABI(decoder: ABIDecoder): PublicKey;
    /** @internal */
    constructor(type: KeyType, data: Bytes | Uint8Array);
    equals(other: PublicKeyType): boolean;
    /**
     * Return Antelope/SYSIO legacy (`SYS<base58data>`) formatted key.
     * @throws If the key type isn't `K1` or `EM`.
     */
    toLegacyString(prefix?: string): string;
    /** Return key in modern Antelope/SYSIO format (`PUB_<type>_<base58data>`) */
    toString(): string;
    toHexString(): string;
    /** @internal */
    toABI(encoder: ABIEncoder): void;
    /** @internal */
    toJSON(): string;
}
//# sourceMappingURL=PublicKey.d.ts.map