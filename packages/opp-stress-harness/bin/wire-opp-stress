#!/usr/bin/env node
const Path = require("path"),
  Assert = require("assert"),
  Fs = require("fs"),
  mainFile = Path.resolve(__dirname, "..", "lib", "out", "cli.js")

Assert.ok(Fs.existsSync(mainFile), `Main file not found @ ${mainFile}.`)

require(mainFile)
