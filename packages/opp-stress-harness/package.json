{
  "name": "@wireio/opp-stress-harness",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "OPP stress observation parsers + bounded-workload chunking consumed by the swap-stress flow",
  "main": "lib/out/index.js",
  "scripts": {
    "compile": "tsc -b tsconfig.json",
    "compile:watch": "tsc -b tsconfig.json -w",
    "build": "pnpm run compile",
    "test": "jest --runInBand --no-cache",
    "clean": "rm -rf lib"
  },
  "dependencies": {
    "@wireio/debugging-shared": "workspace:*",
    "@wireio/test-opp-stress": "workspace:*",
    "ts-pattern": "^5.9.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "25.5.0"
  }
}
