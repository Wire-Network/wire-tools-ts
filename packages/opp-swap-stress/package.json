{
  "name": "@wireio/opp-swap-stress",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "Flow-specific swap-stress saturation logic (deterministic identities, burst/ramp/phase-runner orchestration, flow observation + evidence parsing) consumed by the flow-swap-stress-saturation scenario",
  "main": "lib/out/index.js",
  "scripts": {
    "compile": "tsc -b tsconfig.json",
    "compile:watch": "tsc -b tsconfig.json -w",
    "build": "pnpm run compile",
    "test": "jest --runInBand --no-cache",
    "clean": "rm -rf lib"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.0",
    "@wireio/debugging-shared": "workspace:*",
    "@wireio/opp-stress-harness": "workspace:*",
    "@wireio/opp-typescript-models": "^1.0.26",
    "@wireio/test-opp-stress": "workspace:*",
    "ethers": "^6.14.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "25.5.0"
  }
}
