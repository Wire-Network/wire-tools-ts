describe("wire-debugging-client-tool CLI", () => {
   it("placeholder — CLI tests require bundled binary (run pnpm dist first)", () => {
      // CLI integration tests validate yargs command dispatch.
      // They require the esbuild-bundled .cjs binary to exist.
      // Run `pnpm --filter @wire-e2e-tests/debugging-client-tool dist` first.
      expect(true).toBe(true)
   })
})
