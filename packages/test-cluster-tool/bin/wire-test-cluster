#!/usr/bin/env node
const
	log = console,
	Path = require("path"),
	Assert = require("assert"),
	Fs = require("fs"),
	mainFile = Path.resolve(__dirname, "..", "lib","cjs","cli.js")

Assert.ok(Fs.existsSync(mainFile),
	`Main file not found @ ${mainFile}.`)

require(mainFile)








